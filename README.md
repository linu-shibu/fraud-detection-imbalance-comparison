# fraud-detection-imbalance-comparison
Built and compared 5 ML classifiers (Logistic Regression, Random Forest, XGBoost, SVM, MLP) for credit card fraud detection on a 1.3M-transaction dataset with severe class imbalance (172:1), achieving 0.84 PR-AUC with XGBoost vs. 0.22 for the linear baseline.
